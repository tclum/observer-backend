{
  "name": "observer-backend",
  "version": "1.0.0",
  "description": "Serverless backend for Observer demographic app",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "vitest run tests/e2e"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "express": "^4.18.2",
    "supertest": "^7.0.0",
    "vitest": "^2.0.0"
  }
}
